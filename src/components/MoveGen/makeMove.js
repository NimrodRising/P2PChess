function makeMove(bitboard, move) {
  // what do we need in the move object?
  // we need:
  // 1. To square - we want to pop this bit
  // 2. From Square - we want to update this bit to the
  const newBitboard = bitboard;
  return newBitboard;
}

export default { makeMove };
